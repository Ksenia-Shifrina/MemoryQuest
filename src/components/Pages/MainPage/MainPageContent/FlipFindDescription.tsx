import { Box } from '@mui/material';
import { CustomTypography } from '../../../../helpers/CustomTypography';

export interface FlipFindDescriptionProps {}

const FlipFindDescription: React.FC<FlipFindDescriptionProps> = ({}) => {
  return (
    <Box>
      <CustomTypography></CustomTypography>
    </Box>
  );
};

export default FlipFindDescription;
